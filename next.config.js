/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ['page.tsx', 'api.ts', 'api.tsx'], // Configuração para determinar o que são paginas no projeto
}

module.exports = nextConfig
